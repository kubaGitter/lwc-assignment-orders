public with sharing class Constants {
    
    public static final String ERRMSG_ORDERITEM_DUPLICATEITEM = 'The product is already on the order and cannot be added as a separate item.';
    public static final String ERRMSG_ORDERITEM_ACTIVATEDORDER = 'The order is already activated, no changes are allowed.';
   
}
